name: CI/CD - Build e Deploy no Swarm

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build e Push da imagem da API
        uses: docker/build-push-action@v5
        with:
          context: ./app
          push: true
          tags: gbrhhenri/grupo06-app:latest

      - name: Build e Push da imagem do Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: gbrhhenri/grupo06-frontend:latest

      - name: Atualiza serviços no Swarm via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 100.113.206.49
          username: grupo06
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker service update --image gbrhhenri/grupo06-app:latest grupo06-swarm-stack_app
            docker service update --image gbrhhenri/grupo06-frontend:latest grupo06-swarm-stack_frontend
